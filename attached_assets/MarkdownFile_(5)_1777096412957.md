**Engineer 1 (Platform) — Reporting for Duty**

**Conductor John,** acknowledged. I've read both documents end-to-end. Standing by at the gate.

**Situational Awareness Confirmed**

**Org chart as I understand it:**

*   **You (John)** — Conductor, Human-in-the-Middle, message relay, secrets vault keeper
*   **Architect** — opens/closes gates, no code, spec authority
*   **App Builder (Replit)** — owns the shell at tableicity.com, the 7 worker endpoints, DB schema, admin UI
*   **Me — Engineer 1 (Platform)** — worker loop, claim/heartbeat/runs/review plumbing
*   **Engineers 2 & 3** — Researcher / Analyst (Gate 2, scoped away from me)
*   **Engineer 4** — Halo Adapter (Gate 2.5)
*   **Engineer 5** — Copywriter + SEO/QC (Gate 3)

**Current board state (as of 2026-04-25):**

*   Gate 0 — **CLOSED** by App Builder
*   Gate 1 — **OPEN**, mine
*   Gates 2 → 5 — sealed, hands off

**My Mission (Gate 1, in one breath)**

Build a Python worker loop that claims a dryRun=true job, walks it through all 5 pipeline stages emitting fixture audit rows (zero tokens, zero cost), heartbeats between stages, posts a fixture draft to /review, and lands the job in /admin/newsroom/review as pending — **without calling an LLM, without scraping a single URL, without touching tables that belong to Engineers 2 or 3.**

Plumbing only. No intelligence. No scope creep.

**What I'm Locked Onto**

**The 5 stages, in order:** researcher → data\_analyst → copywriter → seo\_qc → internal\_linker → review (terminal)

**The 7 endpoints I'm authorized to hit:** /claim, /heartbeat, /runs, /jobs/{id} (PATCH), /review, /knowledge (skip), /internal-links (skip or one fixture row)

**The per-stage loop:**

1.  PATCH /jobs/{id} → currentStage = <stage>, status = "running"
2.  Look up agent UUID from newsroom\_agents by role
3.  POST /runs → dryRun: true, status: "completed", tokensUsed: 0, costUsd: 0, fixture input/output
4.  PATCH /jobs/{id} → append stage to agents\_completed
5.  Heartbeat

After 5 stages → POST /review with the exact fixture payload from the brief → shell flips job to completed.

**Cross-Cutting Rules I've Internalized**

1.  **Dry-run is sacred** — bit-identical behavior, zero outbound HTTP except to tableicity.com
2.  **Lease discipline** — 409 from /heartbeat means STOP, no more rows, re-claim. No exceptions.
3.  **Schema wins disputes** — shared/schema.ts is authoritative over any doc
4.  **No silent failures** — required integrations raise and stop; never try/except: pass
5.  **No secrets in code** — NEWSROOM\_WORKER\_SECRET from env only, fail loud if missing
6.  **Audit trail non-negotiable** — every agent invocation = one newsroom\_agent\_runs row
7.  **Stay in my lane** — no writes to newsroom\_source\_documents or newsroom\_lead\_signals (Gate 2), no Halo (Gate 2.5), no LLMs (Gate 3), no pgvector

**What I Need From You, Conductor, Before I Write Code**

To stay disciplined and not burn time guessing, I need three things from your secrets vault / your relay to App Builder:

1.  **NEWSROOM\_WORKER\_SECRET** — the bearer token value. I will only accept it via a secure channel you choose (env var on my side, never in code, never in chat logs that persist).
2.  **Database connection string** for the Postgres instance backing newsroom\_pipeline\_jobs and friends — so I can read agent UUIDs from newsroom\_agents and seed a test dry-run job via the SQL the brief gave me.
3.  **Confirmed base URL** — production https://www.tableicity.com, or are we pointing at a Replit dev URL for Gate 1 verification? Architect's DoD says "prod or the Conductor's chosen test environment" — your call.

Optional but helpful:

*   Access to (or a copy of) John/Newsroom\_Worker\_Contract\_v1.1.md so I can match the exact request bodies for /runs and the PATCH shape for /jobs/{id} against the authoritative API spec rather than inferring from the brief.
*   Confirmation of where /admin/newsroom/review lives in the admin UI so I can do the visual checklist myself before declaring done.

**My Proposed Build Plan (for Architect's awareness, via you)**

**Day 1 — skeleton up, claim working:**

*   Python 3.11 worker, single-file to start (worker/main.py), httpx for HTTP, psycopg for the agent-UUID lookup
*   Env loader → fail loud if NEWSROOM\_WORKER\_SECRET or DATABASE\_URL missing
*   Auth helper that injects Authorization: Bearer … on every request
*   Claim loop with backoff when {job: null}
*   Seed one dry-run job via the SQL from the brief, confirm I can claim it

**Day 2 — full 5-stage walk + lease handling + review post:**

*   Stage iterator over NEWSROOM\_PIPELINE\_STAGES
*   Per-stage: PATCH → lookup agent UUID → POST /runs (fixture in/out, zero tokens) → PATCH agents\_completed → heartbeat
*   Final POST /review with the exact fixture payload (488-char bodyHtml) from the brief
*   409 handler on heartbeat: log, drop the job in memory, re-enter claim loop — no further writes for that job ID
*   Manual test: hit "release lease" in admin UI mid-run, confirm worker stops cleanly

**Verification pass — the 11-item checklist:** I will walk every checkbox in the brief's Definition of Done and the visual verification at /admin/newsroom (dry-run chip on) before I send you "Gate 1 ready for Architect close."

**What I Will NOT Do**

*   Not call Anthropic, OpenAI, or any LLM
*   Not make any outbound HTTP except to the shell host
*   Not write to newsroom\_source\_documents or newsroom\_lead\_signals
*   Not implement Halo, prompt assembly, scraping, embeddings, or pgvector
*   Not implement real Idempotency-Key enforcement beyond a per-request UUID (v1.2's job)
*   Not declare Gate 1 closed myself — only the Architect closes gates

**Standing By**

Conductor, please pass the three items above (secret, DB URL, confirmed base URL) and I'll start coding the moment they land. If the Architect wants a different language than Python (e.g., Node to match the shell), say the word before I lay the first line of code — switching later is wasted work.

**Engineer 1 on station. Awaiting go.**